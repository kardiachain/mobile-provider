# @kardiachain/mobile-provider

This package generates the JavaScript code that's injected on every web page when using KardiaChain Mobile Wallet .

To build this package you can run:

```shell
yarn && yarn build
```
